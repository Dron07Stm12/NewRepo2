<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Scb_Electronmash.MainPage"
             Background="#178080"
             Shell.NavBarIsVisible="True"
             Title="Меню     ВАРТА 16"
             >

    <ScrollView>
        <VerticalStackLayout
            Padding="5,5"
            Spacing="5">
            <!--<Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a hovercraft number nine" />-->

            <Border
               Stroke="#0B0101"
               StrokeThickness="2"
               StrokeShape="RoundRectangle 10"
               BackgroundColor="#b3d6bf"
               Padding="5"
               Margin="5"
               HeightRequest="200">

                <ScrollView x:Name="scrollView" VerticalOptions="Fill">
                    <Label x:Name="label4"
                      Text="receive data"
                      FontAttributes="Bold"    
                      FontSize="14"
                      TextColor="Black"
                      LineBreakMode="WordWrap"
                      VerticalTextAlignment="Start"
                      HorizontalTextAlignment="Start" />
                </ScrollView>

            </Border>

            <Grid Margin="2" ColumnDefinitions="*,*,*" ColumnSpacing="10">


                <Button Margin="5"
                x:Name="CounterBtn"
                Text="Rx" 
                FontAttributes="Bold"             
                FontSize="14"
                TextColor="White"
                BackgroundColor="#1E1033"
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" 
                Grid.Column="0" 
                />


                <Button Margin="5"
                        Text="Clear data"
                        FontAttributes="Bold"   
                        TextColor="#01010d" 
                        BackgroundColor="#b3d6bf"
                        Clicked="OnClear_Data_Dev"                       
                        Grid.Column="1" />





                <Button Margin="5"
                        Text="Tx"
                        FontAttributes="Bold" 
                        FontSize="14"
                        TextColor="#ffffff"
                        BackgroundColor="#082F08"
                        Clicked="OnCounterClicked_Tx"
                        Grid.Column="2"
                  />



            </Grid>


            <!-- Замените ваш текущий CarouselView этим блоком -->
            <CarouselView x:Name="MainCarousel"
              HeightRequest="180"
              IsSwipeEnabled="True"
              IndicatorView="indicators"
              CurrentItemChanged="MainCarousel_CurrentItemChanged"
              VerticalOptions="Start"
              HorizontalOptions="FillAndExpand"
              PeekAreaInsets="0">

                <CarouselView.ItemsLayout>
                    <LinearItemsLayout Orientation="Horizontal"
                           ItemSpacing="0"
                           SnapPointsType="MandatorySingle"
                           SnapPointsAlignment="Center" />
                </CarouselView.ItemsLayout>

                <CarouselView.ItemsSource>
                    <x:Array Type="{x:Type x:String}">
                        <x:String>Страница 1: Пункт A</x:String>
                        <x:String>Страница 2: Пункт B</x:String>
                        <x:String>Страница 3: Пункт C</x:String>
                    </x:Array>
                </CarouselView.ItemsSource>

                <CarouselView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="10" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                            <Label Text="{Binding .}" TextColor="White" FontSize="16" HorizontalOptions="Center" VerticalOptions="Start" LineBreakMode="WordWrap" />
                        </Grid>
                    </DataTemplate>
                </CarouselView.ItemTemplate>
            </CarouselView>

            <IndicatorView x:Name="indicators"
               IndicatorColor="LightGray"
               SelectedIndicatorColor="#361861"
               HorizontalOptions="Center"
               Margin="0,6,0,0" />








        </VerticalStackLayout>
    </ScrollView>

 </ContentPage>